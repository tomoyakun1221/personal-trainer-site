# 本番環境用（docker compose -f docker-compose.prod.yml --env-file .env.production up）
DB_PASSWORD=change_me_strong_password
SECRET_KEY_BASE=run_openssl_rand_hex_64
JWT_SECRET=run_openssl_rand_hex_32
APP_HOST=localhost:8080
CORS_ORIGINS=http://localhost:8080
RUN_DB_SEED=true
PORT=8080
